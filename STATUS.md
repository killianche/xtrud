# STATUS

Точка входа «где мы сейчас». Обновляется в каждом коммите, где была осмысленная единица работы.

---

## Текущее состояние

Подготовительная фаза. Зафиксированы правила работы AI-агента и выбран стек. Ждём ТЗ от другого агента и дизайн от пользователя. Код ещё не написан, git/GitHub не инициализированы.

---

## Что готово

- [x] **2026-05-11** — [CLAUDE.md](CLAUDE.md) переработан: проектный контекст + критические правила инфры (120 строк вместо 534)
- [x] **2026-05-11** — [.claude/rules/working-rules.md](.claude/rules/working-rules.md) — базовые правила работы AI с `paths: ["**/*"]` автозагрузкой
- [x] **2026-05-11** — выбран стек: **Expo universal + Supabase**

---

## В работе сейчас

Ничего активного. Ожидаем:
1. ТЗ от другого агента (детали проекта, фичи, имя)
2. Дизайн от пользователя

---

## Что дальше (приоритет)

1. **Инициализация git + GitHub remote** — после того, как пользователь сообщит имя репозитория
2. **Создание скелета Expo-приложения** — `npx create-expo-app@latest` с TypeScript template, Expo Router, NativeWind
3. **Подключение Supabase** — создать проект в Supabase, сгенерировать `EXPO_PUBLIC_SUPABASE_URL` и `EXPO_PUBLIC_SUPABASE_ANON_KEY`, настроить клиент в `lib/supabase.ts`
4. **Настройка тем (light/dark)** — `useColorScheme` + Tailwind dark variants
5. **GitHub Actions** — typecheck + lint + (позже) tests
6. **EAS конфиг** — для будущих сборок iOS/Android
7. **DESIGN.md** — после получения дизайна от пользователя
8. **Реализация фич по ТЗ** — после получения ТЗ от второго агента

---

## Блокеры

- ⏳ **ТЗ проекта** — другой агент готовит детали; без них нельзя начать кодинг бизнес-логики
- ⏳ **Дизайн** — пользователь пришлёт; без него нет палитры, типографики, компонентов
- ⏳ **GitHub репо** — пользователь должен сказать имя и/или организацию для remote

---

## История ключевых решений

### 2026-05-11 — Стек: Expo universal (web + iOS + Android в одной кодовой базе)

**Выбрано:** Expo SDK 52+ с Expo Router v4, NativeWind, Zustand, TanStack Query, React Hook Form + Zod, Supabase JS SDK, Reanimated 3.

**Деплой:** EAS Build/Submit для mobile, Vercel или Cloudflare Pages для web.

**Альтернативы рассмотрены:**
- **Monorepo Next.js + Expo раздельно** — отброшено: слишком много дублирующейся работы для проекта одного разработчика; переключимся, если ТЗ покажет, что web — это контент-сайт с тяжёлым SEO/блогом
- **Flutter** — отброшено: другая экосистема, web-бандл тяжёлый, менее зрелая интеграция с Supabase
- **Чистый PWA** — отброшено: пользователь хочет нативные приложения в App Store + Play Store

**Условие пересмотра:** если ТЗ покажет, что веб — это в основном маркетинговый сайт / контент с SEO-приоритетом, переходим на monorepo с Next.js. В остальных случаях остаёмся на Expo universal.

### 2026-05-11 — Документация: разбита на короткий CLAUDE.md + детальные `.claude/rules/*.md`

**Выбрано:** короткая точка входа `CLAUDE.md` (~120 строк) + правила в `.claude/rules/working-rules.md` с `paths:` frontmatter для автозагрузки.

**Обоснование:** рекомендация best-practice репо — «keep CLAUDE.md under 200 lines for reliable adherence». Чем длиннее главный файл, тем хуже AI ему следует.

**Альтернатива (отброшена):** оставить один монолитный CLAUDE.md на 534 строки.
