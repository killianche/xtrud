/**
 * /(tabs)/cases/[caseId] — детальный экран работы портфолио ВНУТРИ вкладки
 * «Ваши работы». Сам экран — общий компонент OwnerCaseDetailScreen (тот же
 * рендерит legacy-роут /profile/portfolio/[caseId]). Живя под сегментом cases,
 * экран сохраняет активной вкладку «Ваши работы» и «назад» ведёт в список.
 */
export { default } from "@/features/profile/OwnerCaseDetailScreen";
