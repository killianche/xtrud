# Аудит дизайна и UX — 2026-09-22

> Автор: xtrud-designer. Предел работы — 30 минут (DECISION владельца,
> `.claude/rules/agent-delegation.md` §3.1); отчёт по тому, что успел.
> Код не менялся, commit/push не выполнялись.

## 0. Метод и честные границы

За отведённое время: прочитаны `AGENTS.md`, `design-quality.md`,
`design-enforcement.md`, `docs/IOS_FOUNDATION.md` (включая §9 gap-анализ и §10
чек-лист), `UI_PATTERNS.md` (эталоны, архетипы, anti-patterns),
`docs/UI_ICONS.md` (оглавление), `docs/FIND_SCREEN_REDESIGN.md`,
`docs/TAB_TOPS_REDESIGN.md`, `docs/TAB_TOPS_PRODUCT.md` (оба целиком — это
свежие спецификации от 2026-09-22, решающие ровно ту проблему, которую поднял
владелец); прочитаны все 5 присланных скриншотов сборки 103; прочитаны
целиком `app/(tabs)/find/_layout.tsx`, `app/(tabs)/find/index.tsx`; начало
`app/(details)/orders/[id].tsx`; выполнены ~20 grep-проходов по всему `app/` и
`src/` (хардкод цвета/размера, `Alert.alert`, терминология, тач-цели,
accessibility, состояния, ActivityIndicator, дублирующиеся компоненты).

**Что НЕ было прочитано построчно за 30 минут** (это `UNKNOWN`, не «проверено
и хорошо»): большинство из 78 файлов `app/**`, включая полные тексты
`app/(tabs)/orders/index.tsx`, `app/(tabs)/specialists.tsx`,
`app/(tabs)/index.tsx`, `app/(tabs)/profile/index.tsx`,
`app/(details)/master/[id].tsx`, весь `task-composer`, весь `(auth)`, весь
`(onboarding)`, весь `admin`. Их оценка ниже — по структуре из grep,
импортам и golden-referenced паттернам, не по полному прочтению; там, где это
так, помечено явно. Полный построчный проход всех 78 экранов за 30 минут
физически невозможен — честная альтернатива была бы «додумать», что запрещено
`working-rules.md` §0.

Реального iPhone нет (FACT из задания). Ничего ниже не заменяет проверку на
устройстве — см. раздел 9.

---

## 1. Система дизайна — оценка

**Токены.** `src/lib/colors.ts` → `global.css` через `tokens:check` — единый
источник, это правильная архитектура. Но соблюдение неполное:

- **38 хардкод-хексов** вне `colors.ts` в `app/`+`src/` (grep
  `#[0-9a-fA-F]{3,8}`). Часть — комментарии/референсы (не дефект), но часть —
  реальный код: `src/components/Avatar.tsx:55-62` (палитра фонов инициалов,
  8 хексов мимо токенов), `src/components/OrderRow.tsx:148` и
  `src/features/master-view/SpecialistsListScreen.tsx:48`
  (`shadowColor: "#000000"` — тень мимо токена тени), `src/components/ui/
  FloatingActionButton.tsx:87`, `src/components/ui/GlassButton.tsx:82`
  (обе — `shadowColor: "#000"`), `app/(details)/case/[caseId].tsx:141`
  (`backgroundColor: i === photoIndex ? "#ffffff" : "rgba(255,255,255,0.5)"` —
  индикатор карусели, не адаптируется к теме), `src/features/home/
  PromoBannerCarousel.tsx:98` (`gradient: ["#1f2937","#0f172a"]`),
  `src/features/master-view/availability.ts:41` (`"#10b981"` — «онлайн»-точка
  мимо семантических токенов статуса).
- **17 `rgb(/rgba(` вызовов** вне `colors.ts` — та же категория дефекта
  (оверлеи, тени).
- Ранее зафиксированный в `IOS_FOUNDATION.md` §9.2 п.12 хардкод `tintColor`
  `RefreshControl` (`src/hooks/use-pull-to-refresh.tsx:32`) — **не
  перепроверен в этом проходе**, `UNKNOWN`: исправлен ли к сборке 105.

**Типографика.** `AppText` с системным шрифтом и табличными цифрами — FACT из
§9.1, кап `maxFontSizeMultiplier` снят на основном тексте. `fontSize:` инлайн
встречается 12 раз — в основном внутри `ui/Button.tsx`, `ui/Chip.tsx`,
`ui/Input.tsx`, `ui/Avatar.tsx` (вычисляемые размеры компонента — приемлемо,
это не хардкод мимо системы, а параметризация). Два места вне UI-кита:
`app/(onboarding)/master-categories.tsx:168` (`fontSize: 15`) и
`src/features/home/CinematicHero.tsx:322` (`fontSize: 44, lineHeight: 46`) —
жёсткие числа в экранном коде, не в токене; `CinematicHero` — вероятно герой
главной, где крупный текст не масштабируется с Dynamic Type так же, как
`AppText`, что противоречит §2 «Dynamic Type работает на всём диапазоне» —
требует точечной проверки, не подтверждён на AX-размере (`UNKNOWN`).

**Иконки.** Phosphor используется как основной набор (видно по импортам
`orders/[id].tsx`, `find/_layout.tsx` — комментарии подтверждают следование
`docs/UI_ICONS.md`). Полного grep на «оставшийся Lucide/эмодзи» за время
аудита не сделано — `UNKNOWN`.

**Дублирующиеся механизмы шапки — главный системный дефект.** Одновременно
живут три механизма верха экрана:

1. `LargeTitleBar`/`useLargeTitle` (`src/components/ui/LargeTitle.tsx`,
   374 строки) — используется на 5 файлах `app/`.
2. `ScreenHeader` (`src/components/ui/ScreenHeader.tsx`) — на 9 файлах.
3. Нативный `headerShown:true`/`headerLargeTitle` — 0 файлов сейчас (grep
   пуст), но именно попытка использовать системный large title на
   «Найти задание» в сборке 103 и стала причиной ~280 px пустоты сверху
   (см. раздел 3, скриншот 5) — `app/(tabs)/find/_layout.tsx:4-8` сам
   документирует этот отказ как причину перехода на самописный `LargeTitleBar`.

Это прямое нарушение анти-паттерна «Custom header … Каждый экран свой размер
→ визуальная рассогласованность» из `UI_PATTERNS.md` §6, только не между
кастомными реализациями, а между кастомным и системным. **DECISION уже
принято сегодня** в `docs/TAB_TOPS_PRODUCT.md` §0: «один механизм верха на
все вкладки» — но это решение выбрало самописный `LargeTitleBar`, а не
системный `headerLargeTitle` (потому что системный не заработал внутри
`NativeTabs`). Это техдолг: приложение отказалось от нативного large title в
пользу JS-реализации не по вкусу, а потому что не смогло его завести —
`UNKNOWN`, зафиксирована ли где-то причина именно программного отказа
`headerLargeTitle` внутри `NativeTabs` (баг Expo Router, не наше решение).

**Golden references в UI_PATTERNS.md §1 — устарели.** Проверено кодом:
`app/(tabs)/orders/search/index.tsx`, `.../filters.tsx`,
`.../category-select.tsx`, `app/(tabs)/master/[id].tsx`,
`app/(tabs)/favorites.tsx` — **все перечисленные «эталонные» файлы
отсутствуют** (структура давно переехала: `master/[id]` теперь в
`(details)`, `orders/search` не существует, `favorites` не существует).
Агент, ищущий образец «как делать таб-экран поиска», получит 404 на
единственный документированный эталон. Это не косметика: живой источник
правды для новых экранов сейчас неверен, и именно рассинхрон
документ/код — вероятный канал повторения уже трижды отвергнутых решений
(владелец отклонял версии 1–4 верха «Найти задание» именно потому, что
разные попытки заново изобретали шапку вместо копирования актуального
эталона, которого в документе просто не было).

**Дубли компонентов, подтверждённые):**

- Три кнопочных примитива: `ui/Button.tsx`, `ui/GlassButton.tsx`,
  `ui/FloatingActionButton.tsx` — разделение обосновано (обычная/стеклянная/
  плавающая), не считаю дефектом само по себе, но не проверено, есть ли
  четвёртый локальный вариант «кнопки» внутри `features/*` — `UNKNOWN`.
- Ровно один `EmptyState` (`src/components/EmptyState.tsx`) и один
  `ScreenHeader` — хорошо, без дублей.
- `Skeleton` существует в двух местах: `src/components/Skeleton.tsx` **и**
  `src/components/ui/Skeleton.tsx` — потенциальный дубль механизма загрузки,
  не подтверждено за это время, один ли это ре-экспорт или два разных
  компонента (`UNKNOWN`, файлы не открыты).
- 18 файлов используют `ActivityIndicator` напрямую (grep) при наличии
  специального `Skeleton` — это ровно тот анти-паттерн из `UI_PATTERNS.md`
  §6, первая строка таблицы («`<ActivityIndicator />` посередине пустого
  экрана… выглядит как сломанный экран»). Часть вхождений — внутри самого
  `Skeleton.tsx`/`Button.tsx` (спиннер в состоянии загрузки кнопки —
  допустимо), но `app/(details)/orders/[id].tsx`, `app/(tabs)/profile/
  index.tsx`, `app/(onboarding)/master-photo.tsx`,
  `app/(details)/profile/edit-master.tsx`, `edit-client.tsx`,
  `change-phone.tsx`, `admin/reports.tsx`, `orders/edit/[id].tsx` — 8
  экранных файлов, где раскрутка требует точечной проверки: спиннер на
  весь экран или внутри кнопки (`UNKNOWN` по каждому конкретно, не открыты).

**Терминология.**

- `мастер`/`специалист`: grep пользовательских строк с «мастер» после
  фильтрации имён компонентов/переменных (`MasterCard`, `masterId` и т.п.)
  дал **1 совпадение** — `src/components/EmptyState.tsx:11`: `"Создайте
  первую заявку — мастера откликнутся."` Это единственный найденный
  остаток старого слова в тексте пользователя; везде в остальных местах
  UI-текст уже «специалист» (66 совпадений). Мелкая, но реальная
  несогласованность ровно там, где владелец явно просил замену
  (`docs/TAB_TOPS_PRODUCT.md` §4, «мастер» → «специалист»).
- `задание`/`заказ`/`заявка`: grep даёт 75/19/9 упоминаний соответственно.
  Три слова для одной сущности в пользовательском тексте — не проверено
  построчно, какие именно 19 «заказ» и 9 «заявка» видит конечный
  пользователь против внутренних комментариев/имён (`UNKNOWN`, нужен ручной
  просмотр каждого совпадения — за 30 минут не успел). Но
  `EmptyState.tsx:11` выше — уже подтверждённый пример: «заявка» в тексте,
  который читает клиент, при том что вкладка и почти весь остальной UI
  называют сущность «задание». Рекомендация: свести к одному слову
  («задание» — оно и на вкладке, и в заголовке `LargeTitleBar`).

---

## 2. Верх вкладок / «Найти задание» — детальный разбор (что просил владелец)

Скриншоты 103 подтверждают все три жалобы владельца буквально:

- **Скриншот 5** («Найти задание»): полоса пустого серого пространства
  ~280 px между статус-баром и полем поиска — заголовка нет, место под него
  зарезервировано. `TAB_TOPS_PRODUCT.md` §0 диагностирует причину точно:
  `headerLargeTitle` внутри `NativeTabs` не отрисовался. **FACT, уже
  исправлено к сборке 105**: `app/(tabs)/find/_layout.tsx` явно выключает
  системную шапку и передаёт заголовок в контент через тот же
  `LargeTitleBar`, что на «Мои»/«Специалисты» — единый механизм, как и
  требовало решение. `app/(tabs)/find/index.tsx` использует
  `useLargeTitle(0)` со стартовой высотой 0 «без прыжка на первом кадре (QA
  2026-09-22)» — то есть повторный дефект «прыжка контента» уже пойман и
  закрыт в тот же день.
- **Скриншот 3** («Мои»): закреплённая плашка «Как клиент · 1 / Как мастер»
  с тенью, под ней — почти пустой экран с одной карточкой и большим полем
  серого фона до FAB. `TAB_TOPS_PRODUCT.md` §1.1 предлагает верное решение:
  убрать панель, перенести сегмент в контент как первую строку списка
  (тот же приём, что уже применён к поиску/фильтру на «Найти задание»).
  **Не подтверждено, реализовано ли это к сборке 105** — файл
  `app/(tabs)/orders/index.tsx` не прочитан построчно в этом проходе
  (`UNKNOWN`); commit `67e3850` называется «плавающая капсула ролей», что по
  названию похоже на другое решение (floating segment), чем «убрать панель
  целиком», предложенное в сегодняшнем продуктовом документе — **это
  возможное расхождение между тем, что решил `xtrud-product` сегодня, и тем,
  что уже реализовано вчера/раньше**. Указываю прямо: нужно свести
  `TAB_TOPS_PRODUCT.md` (2026-09-22, рекомендация «убрать панель») с
  фактическим кодом сборки 105 («плавающая капсула» по commit-сообщению) —
  это либо одно и то же другими словами, либо два разных решения одной
  проблемы, принятых в разное время. Решить, какое из них верно, не в моей
  роли за 30 минут без чтения `orders/index.tsx` — фиксирую как открытый
  вопрос владельцу/`xtrud-reviewer`.
- **Скриншот 1** (специалисты, поиск с клавиатурой): категории показаны
  плоским списком с иконками слева — структурно нормально, но при открытой
  клавиатуре первая видимая категория обрезана заголовком секции («Ремонт и
  отделка» с саммари «10 категорий») прямо под полем поиска без отступа —
  не критично, не проверено на AX-размере шрифта (`UNKNOWN`).

**Вывод по разделу:** диагноз владельца был точным (не «личный вкус» —
буквальный баг рендера системного header внутри NativeTabs), и решение от
2026-09-22 (`TAB_TOPS_REDESIGN.md`, `TAB_TOPS_PRODUCT.md`) адресует его
корректно на уровне архитектуры. Ключевой недоделанный кусок — не «Найти
задание» (похоже, готово), а «Мои»/«Специалисты»: не подтверждено, что
закреплённая плашка с тенью реально снята, а не просто перекрашена в
«плавающую капсулу», которая сама по себе тоже была прямо отвергнута
владельцем на скриншоте («серая непонятная плашка») в другой форме.

---

## 3. Топ-10 худших экранов (по видимым структурным сигналам; часть — из
полного чтения, часть — из grep+импортов, помечено)

| # | Экран (файл) | Оценка | Проблема (FACT) | Метод |
|---|---|---|---|---|
| 1 | `app/(tabs)/orders/index.tsx` («Мои») | 2/5 | Скриншот 3: закреплённая плашка с тенью занимает ~200 px, под ней пустота до FAB; не подтверждено исправление в 105 | скриншот + doc |
| 2 | `app/(details)/orders/[id].tsx` (1692 строки) | 2/5 | Один файл держит статус, телефон/WhatsApp/share, отмену, завершение, выбор мастера, admin-скрытие, блокировку пользователя — явный god-screen; смешивает `ActivityIndicator` и `Skeleton` в одном файле (импортирует оба) | чтение начала файла + grep |
| 3 | `src/components/Avatar.tsx` (используется почти всюду) | 3/5 | 8 хардкод-хексов палитры фонов инициалов мимо токенов — нарушение design-enforcement §2 буквально | grep |
| 4 | `UI_PATTERNS.md` §1 golden references (не экран, но управляет всеми экранами) | 1/5 | 5 из 8 эталонных путей физически не существуют в коде — источник правды для новых экранов битый | find |
| 5 | `app/(details)/case/[caseId].tsx` | 3/5 | Индикатор карусели фото хардкодит `#ffffff`/`rgba(255,255,255,0.5)` — не адаптируется к теме (design-enforcement п.3 блокирует приём именно за это) | grep, строка 141 |
| 6 | `src/features/home/PromoBannerCarousel.tsx` | 3/5 | Градиент `["#1f2937","#0f172a"]` мимо токенов на промо-баннере главной (первый экран, который видит каждый) | grep, строка 98 |
| 7 | `src/features/master-view/availability.ts` | 3/5 | Цвет «онлайн»-статуса `#10b981` хардкод — семантический статусный цвет должен быть токеном (сравнимо с `StatusPill`) | grep, строка 41 |
| 8 | `app/(onboarding)/master-categories.tsx` | UNKNOWN/3 | `fontSize: 15` инлайн вне UI-кита — не проверено, ломает ли это Dynamic Type на этой конкретной строке | grep, не дочитан |
| 9 | `src/features/home/CinematicHero.tsx` (главная, герой) | UNKNOWN/3 | `fontSize: 44, lineHeight: 46` жёстко на первом экране приложения — самый заметный текст в продукте потенциально не масштабируется | grep, не дочитан |
| 10 | `app/(details)/profile/services-suggest.tsx` | UNKNOWN/3 | `h-7 w-7` (28 pt) интерактивный элемент без видимого `hitSlop` в той же строке — возможный тач-таргет <44pt | grep, строка 523, не дочитан целиком |

Честно: пункты 8–10 — сигналы из grep, не подтверждённые чтением всего
файла; они кандидаты, не доказанные дефекты. Пункты 1–7 — FACT.

---

## 4. Состояния экрана (design-quality §3)

Grep-снимок наличия паттернов на весь `app/`+`src/`:

- `EmptyState` используется в 8 файлах.
- `Skeleton` — в 22 файлах.
- Текст «Повторить»/`ErrorState` — в 12 файлах.
- Offline-паттерн (`NetInfo`/`isOffline`/«нет соединения» и т.п.) — **0
  совпадений во всём проекте**. Это прямое несоответствие
  `design-quality.md` §3, где оффлайн — обязательное состояние каждого
  экрана. **FACT, не UNKNOWN**: либо оффлайн не спроектирован нигде, либо
  обрабатывается на уровне сети/react-query без явного UI-сообщения
  «Нет соединения» — за 30 минут не нашёл ни одного текстового сообщения
  об оффлайне в пользовательском UI ни одним из проверенных терминов.
  Нужна проверка `xtrud-qa`/`xtrud-technical`, не додумываю причину.
- Соотношение: 8 EmptyState / 78 экранов app/ — даже если часть экранов
  переиспользует один и тот же `EmptyState` на разных вызовах (вероятно),
  разрыв большой. Не проверено поэкранно, какие из 78 файлов вообще имеют
  список, которому нужна пустота (детальные/форменные экраны — не нужен) —
  `UNKNOWN`, но это тот вопрос, который должен закрыть отдельный проход
  QA по состояниям, а не догадка дизайнера.

---

## 5. Accessibility

- `accessibilityRole` встречается в 83 из 146 файлов `.tsx` (app+src) — 57%.
  Для сравнения, `IOS_FOUNDATION.md` §9.1 фиксировал 96/138 в августе — доля
  похожая, но кодовая база выросла, и абсолютный разрыв (63 файла без роли)
  не сократился явно. `UNKNOWN`, вырос ли разрыв в реальности или это
  шумовая разница метрик (не все файлы содержат интерактив).
- `Pressable` — 81 файл; `TouchableOpacity` — 0 (хорошо, единый примитив).
  Не сопоставлено 1:1, какие именно `Pressable` без роли — нужен точечный
  grep `Pressable` без соседнего `accessibilityRole` в том же блоке, не
  успел за 30 минут (`UNKNOWN`).
- Тач-цели: 18 мест с `h-6/7/8/9` (24–36 pt) без явного `hitSlop` в той же
  строке className (grep). Часть из них — вероятно декоративные или уже
  закрыты `hitSlop` отдельным пропом на этой же JSX-ноде (не виден в
  однострочном grep), поэтому не считаю все 18 подтверждённым дефектом;
  но минимум `app/(details)/profile/services-suggest.tsx:523` (`h-7 w-7`,
  28pt) и `src/components/ui/SearchField.tsx:140` (`h-7 w-7`) —
  кандидаты на реальное нарушение design-enforcement (тач-цель <44pt без
  `hitSlop`), требуют точечной проверки `xtrud-qa`.
- Dynamic Type на самом крупном accessibility-размере — не проверено ни на
  одном экране (нет устройства/симулятора в рамках этого прохода).
  `UNKNOWN` целиком, см. раздел 9.

---

## 6. Перцептивная скорость

- **«Найти задание» (сборка 105, после фикса) — хороший пример**: `useLargeTitle(0)`
  со стартовой высотой 0 и комментарием про устранение «прыжка на первом
  кадре» — это ровно требование design-quality §1.2 («лишняя работа не
  делается вовсе»), решённое в тот же день, когда найдено. Похвально.
- **`orders/[id].tsx` смешивает `ActivityIndicator` и `Skeleton`** в одном
  1692-строчном файле — вероятный признак, что часть под-состояний
  (например, подгрузка телефона мастера, действия admin) не получила
  единого skeleton-паттерна и падает на спиннер как более быстрый в
  реализации путь. Это ровно то нарушение, которое `design-quality.md`
  §1.2 называет дефектом «бесконечная загрузка… а не плохая связь» —
  не проверено на факте зависания, но структурный риск подтверждён кодом.
- **Виртуализация**: `IOS_FOUNDATION.md` §9.2 п.8 (август) фиксировал
  `FlashList` только в одном месте. Не переподтверждено на текущий код за
  30 минут — `UNKNOWN`, нужен свежий grep `FlashList` по всему `app/`+`src/`
  (не успел; это отдельная проверка на ~2 минуты, которую стоит поручить
  `xtrud-qa` или сделать в следующем заходе).
- **45× «TypeError: undefined is not a function» / 7× «Maximum update depth
  exceeded» за 30 дней** (из задания владельца, не проверено мной — это
  журнал клиента, к которому у дизайн-роли нет доступа). Единственное, что
  могу отметить по UI-паттерну: `orders/[id].tsx` — самый большой и самый
  многофункциональный экран из всех виденных (1692 строки, минимум 8
  доменных хуков: `useOrderDetail`, `useCancelOrder`, `useDeleteOrder`,
  `useCompleteOrder`, `usePickOrderMaster`, `useMasterPublicProfile`,
  `useMasterPhone`, `useMarkOrderNotificationsRead`, `useBlockUser`,
  `useAdminHideOrder` — и это только импорты в первых 60 строках). Экран с
  таким числом независимых хуков состояния — статистически наиболее
  вероятное место для случайного цикла ре-рендера («Maximum update depth
  exceeded»), если хотя бы один `useEffect` реагирует на объект/массив без
  мемоизации. Это гипотеза для `xtrud-technical`, не диагноз — я не читал
  тела хуков и не бегал профайлер.

---

## 7. Сильные стороны — не ломать

- Единая генерация токенов `colors.ts → global.css` с проверяемым `tokens:check`
  — правильная база, дефекты выше — точечные хардкоды поверх неё, не
  архитектурная проблема.
- Единый `EmptyState`, единый `ScreenHeader`, отсутствие дублей
  `TouchableOpacity` — дисциплина примитивов лучше типичного RN-проекта.
  system font + табличные цифры в `AppText` — реализовано системно, не
  точечно.
- Быстрая реакция на диагноз владельца: жалоба на скриншотах сборки 103 →
  в тот же день (2026-09-22) есть продуктовый разбор
  (`TAB_TOPS_PRODUCT.md`), дизайн-разбор (`TAB_TOPS_REDESIGN.md`) и уже
  закоммиченный фикс с QA-комментарием про устранение прыжка кадра
  (`find/index.tsx`). Цикл «жалоба → корневой диагноз → фикс» работает
  быстро — это ценнее любого частного визуального решения, сохранить как
  процесс.
- Detail-экраны — в root native Stack, не скрытые табы (§9.1
  `IOS_FOUNDATION.md`) — edge-swipe работает нативно, это FACT из
  канонического документа, не переопровергнуто ничем увиденным сегодня.

---

## 8. План «в два раза» — по влиянию

### Сейчас (2 недели)

1. **Свести golden references в `UI_PATTERNS.md` §1 с реальным деревом
   `app/`.** Проблема: 5/8 эталонных путей не существуют (раздел 1).
   Решение: заменить на актуальные (`app/(tabs)/find/index.tsx`,
   `app/(tabs)/orders/index.tsx`, `app/(details)/master/[id].tsx` и т.д.),
   один заход по всему документу. Эффект: агенты (включая меня в будущих
   заходах) перестают изобретать шапку заново — прямая причина того, что
   владелец видел 4 разных отвергнутых версии одного экрана. Стоимость: S.
2. **Свести `TAB_TOPS_PRODUCT.md` (убрать панель) с фактическим кодом
   сборки 105 («плавающая капсула») на «Мои»/«Специалисты».** Проблема:
   раздел 2 — расхождение между сегодняшним решением и вчерашним commit
   по названию. Решение: `xtrud-reviewer` читает `orders/index.tsx` и
   `specialists.tsx` целиком, сверяет с `TAB_TOPS_PRODUCT.md` §1, либо
   подтверждает соответствие, либо переоткрывает вопрос владельцу с двумя
   вариантами и ценой каждого. Эффект: закрывает жалобу владельца
   («плавающая капсула» уже была отдельно отвергнута им же на другом
   элементе — риск повторить тот же паттерн). Стоимость: S (чтение +
   сверка), возможен M если нужна повторная правка кода.
3. **Убрать 7 подтверждённых хардкод-цветов** (`Avatar.tsx` палитра,
   `OrderRow.tsx`/`SpecialistsListScreen.tsx`/`FloatingActionButton.tsx`/
   `GlassButton.tsx` тени, `case/[caseId].tsx` индикатор карусели,
   `PromoBannerCarousel.tsx` градиент, `availability.ts` онлайн-точка) на
   токены. Проблема: design-enforcement §2 — блокер приёма буквально.
   Эффект: тёмная тема перестаёт «плыть» в этих 7 местах (карусель фото и
   промо-баннер — оба на первом экране/в первых тапах пользователя).
   Стоимость: S.
4. **Единый термин вместо «задание/заказ/заявка».** Проблема: 75/19/9
   упоминаний. Решение: ручной проход по всем «заказ»/«заявка» в
   пользовательских строках (не в коде/типах), замена на «задание»,
   включая `EmptyState.tsx:11` («мастера» → «специалисты» заодно, тот же
   файл). Эффект: один из «несогласованных текстов», который явно мешает
   пониманию — правило владельца «текст короткий и без жаргона» включает
   «без разнобоя терминов». Стоимость: S.
5. **Проверить оффлайн-состояние — 0 совпадений в проекте.** Проблема:
   design-quality §3 требует явного UI на оффлайне, grep не нашёл ни одного.
   Решение: `xtrud-qa` выключает сеть на 3 ключевых экранах (Главная,
   Найти задание, экран задания) и фиксирует факт — либо это уже работает
   через react-query error state молча, либо реально не спроектировано.
   Эффект: закрывает белое пятно раздела design-quality, которое сейчас
   никем не проверено. Стоимость: S (проверка) + M (если нужен UI).

### Следующее (1–2 месяца)

6. **Разбить `app/(details)/orders/[id].tsx` (1692 строки, 10+ хуков
   состояния).** Проблема: раздел 3 п.2, раздел 6 — вероятный источник
   каскадных ре-рендеров, единая точка отказа для самого важного экрана
   продукта (карточка задания). Решение: вынести блоки (действия owner,
   действия master, admin-панель, карусель фото) в отдельные
   presentational-компоненты с явными пропами вместо общего состояния
   экрана; это епархия `xtrud-technical`+`xtrud-implementer`, не только
   дизайн, но именно размер и смешение ответственности — дизайн-дефект
   («одно главное действие» — здесь их минимум пять на одном экране).
   Эффект: снижает риск «Maximum update depth exceeded» на самом
   посещаемом detail-экране; ускоряет TTI. Стоимость: L.
7. **Слить `Skeleton.tsx` (2 файла) и заменить оставшиеся 8 голых
   `ActivityIndicator` на скелетоны формы контента.** Проблема: раздел 1,
   раздел 6. Эффект: устраняет «выглядит как сломанный» момент загрузки на
   8 экранах, включая редактирование профиля (частый путь). Стоимость: M.
8. **Проверить Dynamic Type на `CinematicHero.tsx` (герой главной,
   `fontSize: 44` жёстко) и `master-categories.tsx` (`fontSize: 15`).**
   Проблема: раздел 3 пп.8-9 — оба жёстких размера не через `AppText`/токен.
   Эффект: первый текст, который видит новый пользователь, либо
   подтверждается как корректно масштабируемый, либо чинится. Стоимость: S
   (проверка) + S (правка, если подтвердится).
9. **Аудит 61 вызова `Alert.alert`** (grep) против общего адаптера
   `src/lib/confirm.ts` (`CROSS_PLATFORM_RULES.md` §3 запрещает прямой
   `Alert` при наличии адаптера). Проблема выросла с 32 (август,
   `IOS_FOUNDATION.md` §9.2 п.17) до как минимум 40 вне `confirm.ts` (этот
   grep) — направление не «устранение техдолга», а рост. Эффект: неизвестно
   без разбора каждого вызова, сколько — оправданный 3+-кнопочный случай
   (документированное исключение), сколько — обход адаптера. Стоимость:
   M (аудит) + переменная (правка).
10. **Точечная проверка тач-целей `h-7 w-7`/`h-8 w-8` (18 мест из grep)
    на реальный `hitSlop`.** Эффект: закрывает потенциальный accessibility-
    блокер до релиза, где он ещё не подтверждён точно. Стоимость: S.

### Потом

11. Единая система: закрепить `LargeTitleBar` как единственный механизм
    верха вкладок (уже решено сегодня продуктом/дизайном) и вынести это
    решение в `UI_PATTERNS.md` §1 как новый golden reference —
    самостоятельная задача после того, как пункт 1 и 2 закрыты и
    подтверждены на реальном устройстве.
12. Решить судьбу `PhoneFrame` в корневом `Stack` (`IOS_FOUNDATION.md`
    §9.2 п.13, не переподтверждено сегодня) — вынести web-специфичный слой
    из критического пути iOS-навигации, если он всё ещё там.
13. Полный accessibility-проход (VoiceOver сценарии, не только grep
    `accessibilityRole`) — только после появления реального устройства.

### Единая система компонентов — что оставить эталоном

- **Оставить эталоном**: `AppText`, единый `EmptyState`, единый
  `ScreenHeader` (для detail-экранов), `LargeTitleBar`/`useLargeTitle` (для
  вкладок, после сверки п.2 плана), `src/lib/colors.ts` как единственный
  источник цвета.
- **Слить**: `src/components/Skeleton.tsx` + `src/components/ui/Skeleton.tsx`
  — один компонент, один путь импорта (после проверки, не два ли это разных
  назначения).
- **Убрать точечно, не как класс**: голые `ActivityIndicator` вне
  `Button`/индикатора кнопки — заменить на skeleton формы контента там, где
  сейчас есть полноэкранный спиннер.
- **Актуализировать, не удалять**: `UI_PATTERNS.md` §1 — обновить пути, не
  трогая сам принцип «копируй эталон, не изобретай».

---

## 9. Что обязательно проверить на реальном iPhone

Устройства нет — весь список ниже `UNKNOWN` до физической проверки:

1. «Найти задание»/«Мои»/«Специалисты» — реальная высота `LargeTitleBar`
   при первом кадре (скриншоты подтверждают только старый баг сборки 103,
   не новое поведение 105).
2. Dynamic Type на AX3/AX5, отдельно `CinematicHero.tsx` и любые инлайн
   `fontSize:` в экранном коде (не в UI-ките).
3. VoiceOver полный проход по `orders/[id].tsx` — самый насыщенный
   действиями экран (звонок, WhatsApp, share, отмена, завершение, выбор
   мастера, жалоба, блокировка).
4. Reduce Motion на анимациях `LargeTitleBar` (compact-переход,
   `compactTitleOpacity`) и `PortfolioLightbox`.
5. Оффлайн-поведение (раздел 4) — есть ли вообще видимый UI-сигнал.
6. Тач-цели 18 найденных `h-7/8/9` кандидатов — визуально и через
   Accessibility Inspector.
7. Клавиатура на «Найти задание» (скриншот 1) — не обрезает ли первую
   категорию под полем поиска.
8. Реальная частота «Maximum update depth exceeded» именно на
   `orders/[id].tsx` — сопоставить с логом ошибок (владение —
   `xtrud-technical`, не дизайн, но экран для проверки — этот).

---

## Отчёт (FACT / DECISION / UNKNOWN) — сводка контракта

Каждый пункт разделов 1–8 промаркирован по месту как FACT (код/grep/скриншот),
DECISION (ссылка на `TAB_TOPS_PRODUCT.md`/`TAB_TOPS_REDESIGN.md`/
`FIND_SCREEN_REDESIGN.md`/`design-quality.md`) или UNKNOWN (файл не прочитан
целиком за отведённые 30 минут — указан способ проверки). Ни один пункт не
подставлен по памяти или по аналогии с другим iOS-приложением: там, где не
хватило времени открыть файл, стоит `UNKNOWN`, а не правдоподобная оценка.
